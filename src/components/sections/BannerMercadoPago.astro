---
import ContainerCenter from "@components/ContainerCenter.astro";
import { creditCardsMercadoPago, phoneCompanies } from "@data/dataInfo";
import { Image } from "astro:assets";
import CardPayment from "@assets/credit-cards/card-payment.svg";
import LogoMP from "@assets/credit-cards/logo-mercado-pago-center.svg";
import imgPhone from "@assets/phone-companies/phone-companies.svg";
---

<section
  class="flex justify-center items-center w-full min-h-screen"
  aria-labelledby="mercado-pago-title"
>
  <ContainerCenter class="flex flex-col laptop:flex-row gap-6">
    <article
      class="laptop:w-[70%] bg-gradient-to-br from-[#009EE3] to-[#0073CF] rounded-xl relative p-6 text-white"
      data-aos="fade"
      role="region"
      aria-labelledby="mercado-pago-title"
    >
      <header class="relative mb-6">
        <h1 id="mercado-pago-title" class="text-4xl laptop:text-7xl font-bold">
          ¡Paga fácil y rápido con Mercado Pago!
        </h1>
        <Image
          src={LogoMP}
          alt="Logo de Mercado Pago"
          class="w-32 h-32 object-cover absolute -top-4 -right-8"
          loading="lazy"
        />
        <p class="text-white/90 laptop:w-1/2 laptop:text-2xl mt-4">
          En Servitec Halachó, tus compras son sin complicaciones
        </p>
      </header>

      <figure>
        <Image
          src={CardPayment}
          alt="Ilustración de pago con tarjeta"
          class="laptop:w-xl laptop:h-xl object-cover m-0 laptop:absolute top-44 right-0"
          loading="lazy"
        />
      </figure>
    </article>

    <aside class="laptop:w-[30%] flex flex-col gap-6">
      <section
        class="rounded-xl bg-[#f5f5f7] p-6 text-black"
        data-aos="fade-up"
        aria-labelledby="titulo-tarjetas"
      >
        <h2 id="titulo-tarjetas" class="text-2xl font-bold">
          🪪 ¡Ahora es más fácil pagar!
        </h2>
        <p class="my-2">
          Aceptamos tarjetas de crédito de los bancos más confiables. 🎉
        </p>
        <div
          class="grid grid-cols-3 place-items-center gap-2 w-full"
          role="list"
        >
          {
            creditCardsMercadoPago.map((card) => (
              <figure id={card.name} role="listitem">
                <Image
                  src={card.image}
                  alt={`Tarjeta aceptada: ${card.name}`}
                  class="w-12 h-12 object-contain"
                  loading="lazy"
                />
              </figure>
            ))
          }
        </div>
        <p class="text-sm my-2">
          ¡No esperes más y disfruta de nuestros servicios con tu tarjeta! 🙌
        </p>
      </section>

      <section
        class="rounded-xl bg-[#f5f5f7] p-6 text-black"
        data-aos="fade-up"
        data-aos-offset="100"
        aria-labelledby="titulo-recargas"
      >
        <h2 id="titulo-recargas" class="text-xl font-bold">
          ¡Recarga en segundos, conecta sin límites!
        </h2>
        <p class="my-2">
          Servitec tiene las mejores opciones para ti. 📳 Recargar nunca fue tan
          fácil 🤳
        </p>
        <div class="flex w-full gap-2">
          <figure class="w-1/2">
            <Image
              src={imgPhone}
              alt="Ilustración de recarga telefónica"
              loading="lazy"
            />
          </figure>
          <div class="grid grid-cols-2 place-items-center gap-2 w-1/2">
            {
              phoneCompanies.map((company) => (
                <figure id={company.name}>
                  <Image
                    src={company.image}
                    alt={`Compañía: ${company.name}`}
                    class="w-14 h-14 object-contain"
                    loading="lazy"
                  />
                </figure>
              ))
            }
          </div>
        </div>
        <p class="text-sm my-2">
          Otras compañías disponibles: Bait, Pillofon, MiMóvil, Oui, Axios
          Mobile, JR Móvil. 🙌
        </p>
      </section>
    </aside>
  </ContainerCenter>
</section>
